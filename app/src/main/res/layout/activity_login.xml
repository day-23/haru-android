<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:background="@drawable/login_background">
    <ImageView
        android:id="@+id/login_haru_title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/haru_start_logo"
        tools:ignore="MissingConstraints" />
    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="나의 하루를 가치있게 만드는 습관\n지금 하루와 함께 시작해 보세요!"
        android:gravity="center"
        android:textSize="20dp"
        android:textColor="#FDFDFD"
        app:layout_constraintBottom_toBottomOf="@+id/login_haru_title"
        android:layout_marginBottom="30dp"/>
    <Button
        android:id="@+id/login_btn"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        app:layout_constraintBottom_toBottomOf="parent"
        android:layout_marginBottom="368dp"
        android:gravity="center"
        android:text="login"
        android:layout_marginHorizontal="38dp"
        android:padding="20dp"
        />
    <TextView
        android:layout_width="172dp"
        android:layout_height="40dp"
        android:background="@drawable/login_ballon"
        android:text="모모는 떠났어!"
        android:textSize="20dp"
        android:textColor="#1DAFFF"
        android:textStyle="bold"
        android:gravity="center"
        app:layout_constraintRight_toRightOf="parent"
        app:layout_constraintBottom_toTopOf="@+id/login_btn"
        android:layout_marginRight="54dp"
        android:layout_marginBottom="32dp"
        tools:ignore="MissingConstraints" />
    <ImageView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/login_character_a"
        app:layout_constraintBottom_toBottomOf="parent"
        tools:ignore="MissingConstraints" />
</androidx.constraintlayout.widget.ConstraintLayout>